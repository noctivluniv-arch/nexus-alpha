[build]
  command = "cd artifacts/nexusalpha && pnpm install --no-frozen-lockfile && pnpm exec expo export --platform web"
  publish = "artifacts/nexusalpha/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.30.3"
